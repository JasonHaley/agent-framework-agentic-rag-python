# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: agent-framework-agentic-rag-python
metadata:
    template: azd-init@1.23.2
services:
    app:
        project: app
        language: python
        host: containerapp
        docker:
            remoteBuild: true
resources:
    app:
        type: host.containerapp
        port: 80
